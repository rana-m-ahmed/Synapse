# =============================================================================
# Synapse Backend — Python Dependencies
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9          # Required for file uploads (multipart/form-data)

# --- Supabase ---
supabase>=2.10.0
python-jose[cryptography]>=3.3.0 # JWT decoding & verification

# --- LLM & RAG ---
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-community>=0.3.0
langchain-core>=0.3.0

# --- Embeddings (local, free, no API cost) ---
sentence-transformers>=3.0.0

# --- Document Parsing ---
pypdf>=4.0.0                     # PDF text extraction
python-docx>=1.1.0               # DOCX text extraction
openpyxl>=3.1.0                  # Excel (.xlsx) parsing
beautifulsoup4>=4.12.0           # URL scraping — HTML to text
httpx>=0.27.0                    # Async HTTP client for URL fetching

# --- Configuration ---
pydantic-settings>=2.5.0         # Typed env var management
python-dotenv>=1.0.0             # .env file loading

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0
